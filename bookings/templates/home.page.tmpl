{{template "base" .}}

{{define "content"}}
<div class="container">
  <div class="row">
    <div class="col">
      <p>Home Page</p>
      <p>This is from the template : {{index .IntMap "amount"}}</p>
    </div>
  </div>
</div>
{{ end }}
